# extract fields and columns
# see:
# https://github.com/tldr-pages/tldr
# https://tldr.ostera.io/cut

# To cut out the third field of text or stdoutput that is delimited by a #:
cut -d# -f3

# see : https://shapeshed.com/unix-cut/
# cut out first 5 bytes
echo 'abcdefghi' | cut -b 1-5
# cut out bytes 1,5
echo 'abcdefghi' | cut -b 1,5

# cut by character (♣ = 3 bytes)
echo '♣foobar' | cut -c 1-4

# assume file 'name.csv' with following contents
John,Smith,34,London
Arthur,Evans,21,Newport
George,Jones,32,Truro

# use ',' as delimiter, cut out field 2,4
cut -d ',' -f 2,4 names.csv
Smith,London
Evans,Newport
Jones,Truro

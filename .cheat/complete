# see the completions at: /usr/local/share/fish/completions for examples
# see: /usr/local/share/fish/completions/grep.fish
# see: https://devhints.io/fish-shell
# see: http://fishshell.com/docs/current/commands.html#complete

# add start stop arguments
complete -c myprog -a 'start stop'

# add completions for
#   abc -o yes|no
#   abc --output=yes|no
complete -c abc -a 'start stop' -s o -l output -a "yes no"

# add completiosns for pids
complete -c abc -a '(__fish_complete_pids)'

# add completions for all hostnames
complete -c xyz -a '(__fish_print_hostnames)'

# internal completions

__fish_complete_directories STRING DESCRIPTION
    performs path completion on STRING, allowing only directories, and giving them the description DESCRIPTIO

_fish_complete_path STRING DESCRIPTION
    performs path completion on STRING, giving them the description DESCRIPTION

__fish_complete_groups
    prints a list of all user groups with the groups members as description.

__fish_complete_pids
    prints a list of all processes IDs with the command name as description.

__fish_complete_suffix
    SUFFIX performs file completion allowing only files ending in SUFFIX, with an optional description.

__fish_complete_users
    prints a list of all users with their full name as description.

__fish_print_filesystems
    prints a list of all known file systems. Currently, this is a static list, and not dependent on what file systems the host operating system actually understands.

__fish_print_hostnames
    prints a list of all known hostnames. This functions searches the fstab for nfs servers, ssh for known hosts and checks the /etc/hosts file.

__fish_print_interfaces
    prints a list of all known network interfaces.

__fish_print_packages
    prints a list of all installed packages. This function currently handles Debian, rpm and Gentoo packages.

 ***
# for bash, see: https://www.thegeekstuff.com/2013/12/bash-completion-complete/
export IFS=" "
complete -W "bubble quick" ./sort_numbers.sh
# tell bash that the function _known_hosts should be used to handle the completion
# of arguments to the xvncviewer.
complete -F _known_hosts xvncviewer


# visual studio code
# Goto Symbol in Workspace not working?
# use `brew install ctags`.  see: https://github.com/Microsoft/vscode-python#optional-steps
# if Symbols not working, delete .vscode/tags file.

Index:
 * code-view

⌘⌥ K        # Bookmarks toggle
⌘⌥ J/L      # Bookmarks prev/next

# Code Navigation
# https://code.visualstudio.com/Docs/editor/editingevolved
## peek
⬆️  F12      # Find all references
⌥  F12      # Peek definition

# to use vscode with pipenv:
$ pipenv --venv
> /Users/user/path/to/virtualenv/
# append '/bin/python' to the above path.
# Edit .vscode/settings.json
{
    "python.linting.pep8Enabled": true,
    "python.linting.flake8Enabled": true,
    "python.pythonPath": "/Users/user/path/to/virtualenv/bin/python",
}


#Help
ctrl ]          open help topic

# Basic modes
ESC             command/normal mode
i               insert/edit mode

# File management

:e              reload file
:q              quit
:q!             quit without saving changes
:w              write file
:w {file}       write new file
:wq             write file and exit
:x              write file and exit

# Movement

    k
  h   l         basic motion
    j

  ()            jump sentence
  {}            jump paragraph
  gg/G          jump begin/end of file

  H/L           jump to top/bottom of screen
  M             jump to middle of screen

CTRL O/I        goto prev/next cursor position

w               next start of word
W               next start of whitespace-delimited word
e               next end of word
E               next end of whitespace-delimited word
b               previous start of word
B               previous start of whitespace-delimited word
0               start of line
$               end of line
gg              go to first line in file
G               go to end of file
gk		move down one displayed line
gj		move up one displayed line

# Insertion
#   To exit from insert mode use Esc or Ctrl-C
#   Enter insertion mode and:

a               append after the cursor
A               append at the end of the line
i               insert before the cursor
I               insert at the beginning of the line
o               create a new line under the cursor
O               create a new line above the cursor
R               enter insert mode but replace instead of inserting chars
:r {file}       insert from file

# Editing

u               undo
yw              yank word
yy              yank (copy) a line
y{motion}       yank text that {motion} moves over
p               paste after cursor
P               paste before cursor
<Del> or x      delete a character
dd              delete a line
d{motion}       delete text that {motion} moves over

# Search and replace with the `:substitute` (aka `:s`) command

/               search forward
?               search backwards
:s/search/replace/	    replace the first match on the current line only
:s/search/replace/g	    replace all matches (`g` flag) on the current line only

:8,10s/search/replace/g    replace within line range
:%s/search/replace/g	    replace all matches in the entire file (`:%s`)
:%s/search/replace/gc	    ask to manually confirm (`c` flag) each replacement

# Preceding a motion or edition with a number repeats it 'n' times
# Examples:
50k         moves 50 lines up
2dw         deletes 2 words
5yy         copies 5 lines



42G         go to line 42

# options
set nu/nonu             # no numbers; for pasting.
set paste/nopaste       # change paste option



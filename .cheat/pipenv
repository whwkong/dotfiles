# https://github.com/pypa/pipenv
pipenv shell            # activate project's virtualenv

pipenv shell --fancy    # run shell in fancy mode; prompt is no longer modified
    # if 'pipenv shell --fancy' is not working then just use 'pipenv shell'
    # see: https://pipenv.readthedocs.io/en/latest/basics/#about-shell-configuration

pipenv install --dev <package>

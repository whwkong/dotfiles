# as grep was, so was ack, but faster  :)
# https://www.mankier.com/1/ag
# add files to .ignore: eg *.min.js
# PCRE (Perl Compatible Regex) - escape these characters: 
# . ^ $ * + - ? ( ) [ ] { } \ |

-w --word-regexp        # Only match whole words

-i --ignore-case        # Match case-insensitively

-A --after [LINES]      # Print lines after match (Default: 2)

-B --before [LINES]     # Print lines before match (Default: 2)

-U --skip-vcs-ignores   # Ignore VCS ignore files
                        #  (.gitignore, .hgignore; still obey .ignore)

-Q --literal            Don't parse PATTERN as a regular expression

--list-file-types


ag searchterm -G js$     # Only search Javascript files.
                         # -G allows for regular expressions


# see : http://minimul.com/ignoring-files-with-ag-silver-searcher.html
ag PATTERN --ignore '*PATTERN'      # ignore files, directories
                                    # ignore doesn't take regex
ag PATTERN --ignore '*.json'        # ignore json files, use SINGLE QUOTES

# search two words near each other
grep -ERHn "\bPATTERN11\W+(\w+\W+){1,10}PATTERN2"
grepnear word1 word2
agnear word1 word2 COUNT    # where COUNT = number of words apart, default = 10
